{
	"datasources": [ 
  		{
  			"name": "mysql1",
  			"type": "mysql",
  			"connection": {
  				"host": "localhost",
    			"user": "root",
    			"password": "password",
    			"database": "test"
    		},
    		"permissions": [
    			{
    				"roles": ["list of roles that can/cannot query this datasource"]
    			},
    			{
    				"users" : ["list of users who can/cannot query this datasource"]
    			}
  			]
  		},	
  		{
  			"name": "mysql2"
  		}
  	]
}